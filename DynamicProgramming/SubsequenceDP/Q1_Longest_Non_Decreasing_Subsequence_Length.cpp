/*
    Given an array of integers.
    We need to find the length of the longest non-decreasing subsequence in this array.

            1 <= N <= 10^6
            -10^9 <= A[i] <= 10^9 
*/

#include <iostream>
#include <vector>


int main() {
    size_t size;
    std::cout << "\nEnter the number of elements in the "
}